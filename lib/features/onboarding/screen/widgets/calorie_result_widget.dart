import 'package:flutter/material.dart';

class CalorieResultWidget extends StatelessWidget {
  const CalorieResultWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Calorie Result'));
  }
}
